[project]
name = "blog-playwright"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8",
  "pytest-playwright>=0.6",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
